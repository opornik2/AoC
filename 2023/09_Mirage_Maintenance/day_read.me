tags: extrapolation
corner cases: negative values
algo:
problems: copied invalid input (some lines pased wrongly), copied without one line (199 vs 200), using of abs() that changed drastically output

