tags: mapping ranges of numbers, regex parsing, input parsing
edge cases: for real input huge ranges/amount of numbers causes Out of Memory
problems: 
  - need to invent mapping of ranges and not of sinlge numbers from the ranges (see edge cases)
  - lists formatted as (start, range) so need parsing in pairs
  - part2 output gives wrong result!
improve: parse input in a shorter and neater way (split by \n\n, assign headers to rest of data)
algo:
