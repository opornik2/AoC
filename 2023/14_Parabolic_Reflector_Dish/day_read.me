tags: matrix transpose, rotate, move matrix elements
edge cases:
problems: 
  - modify list of tuples (our matrix)
  - convert list of string to list of lists (but not map)
  - count number of elements in list using numpy
  - part2 requires 1 billion operations
  - find repeating pattern - store last grids in history
  - apply the pattern (loop) to 1 billion operations and count the right value
improve:
algo:

